import styled from 'styled-components';

const StyledOverviewContainer = styled.div`
  display: flex;
`;

const Half = styled.div`
height: inherit;
width: 50%;
`;

export default { StyledOverviewContainer, Half };
